<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <link rel="stylesheet" href="/css/styles.css">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Guia de Jogos</title>
</head>

<body>
    <header></header>
    dados: <%= resultado[0].url %>
        <section>
            <article>
                <ul>
                    <% for (i=0; i<resultado.length; i++) { %>
                        <li> <img src="<%= resultado[i].url %>" alt="capa <%= resultado[i].title %>"> </li>
                        <% } %>
                </ul>
            </article>
        </section>



        <footer></footer>
</body>

</html>